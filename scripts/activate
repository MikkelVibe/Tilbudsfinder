#!/usr/bin/env bash

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

case ":$PATH:" in
    *":$ROOT_DIR/bin:"*) ;;
    *) export PATH="$ROOT_DIR/bin:$PATH" ;;
esac

echo "Tilbudsfinder local tools active. You can now use: php, composer"
